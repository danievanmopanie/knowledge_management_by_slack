[Unit]
Description=Daily 16:00 Frontend Support afternoon handover
Requires=frontend-afternoon-report.service

[Timer]
# Every day at 16:00 local time (SAST on the GX10 if timezone is set)
OnCalendar=*-*-* 16:00:00
Persistent=true

[Install]
WantedBy=timers.target
